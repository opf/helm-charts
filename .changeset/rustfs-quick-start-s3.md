---
"openproject": minor
---

Add `rustfs.bundled` option to deploy a minimal, single-node [RustFS](https://rustfs.com) instance and automatically configure it as S3-compatible attachment storage. By default, only intended as a quick way to get S3 storage working out of the box; not meant for production use.
